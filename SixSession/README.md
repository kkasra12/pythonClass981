# Tkinter

```python
import tkinter as tk
window = tk.Tk()
window.title("Python981")
window.mainloop()
```

## Label

```python
from tkinter import * 
window = Tk()
window.title("Python981")
lbl = Label(window, text="Hello")
lbl.grid(column=0, row=0)
window.mainloop()
```
